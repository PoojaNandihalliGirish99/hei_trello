/** @type {import('next').NextConfig} */
const nextConfig = {
    images:{
        domains:['cloud.appwrite.io','links.papareact.com', 'main--chimerical-crumble-974f59.netlify.app']
    }
}

module.exports = nextConfig
